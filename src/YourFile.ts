import { Server } from 'http';

class YourClassName {
    private server: Server;

    constructor() {
        this.server = new Server(); // Initialize the server property
    }

    // ...existing code...
}
